{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Yuri Rewrite window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
