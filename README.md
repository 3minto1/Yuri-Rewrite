# Yuri Rewrite

Yuri Rewrite 是一款 Windows 本地桌面工具，用来导入 TXT 小说，调用你自己配置的 AI 模型分析原文，并把符合条件的章节改写为双女主 / 百合版本。

小说正文、改写稿、模型配置、任务日志、导出文件和 API Key 都保存在本机。程序本身不提供公共 AI 服务，使用前需要准备自己的模型账号和 API Key。

## 下载与运行

1. 打开 GitHub 仓库的 [Releases](https://github.com/3minto1/Yuri-Rewrite/releases) 页面。
2. 下载 `YuriRewrite-v...-windows-x64.zip` portable 压缩包。
3. 解压到一个可写目录。
4. 双击 `Yuri Rewrite.exe` 启动。

程序目前没有代码签名。Windows SmartScreen 如果提示“未知发布者”，请确认文件来自本仓库 Releases 页面，再选择继续运行。

## 开始前准备

你需要准备：

- Windows x64 系统。
- UTF-8、GBK 或常见中文编码的 TXT 小说。
- 一个可用的 AI 服务账号、API Key、Base URL 和模型 ID。
- 稳定网络，以及足够的模型额度。

支持的模型接口：

- OpenAI-compatible Chat Completions：适用于 OpenAI、DeepSeek、豆包 / 火山方舟、Moonshot / Kimi、小米 MiMo、SiliconFlow 等兼容服务。
- Gemini：使用 Google Gemini 原生接口。

不同服务商的模型 ID、价格、并发、TPM/RPM 限制和参数支持会变化，请以服务商当前文档为准。

## 快速上手

1. 点击“导入 TXT”，软件会自动识别章节，并按批次整理。
2. 先配置模型，填写 Base URL、模型 ID 和 API Key，保存后点击“诊断模型”。
3. 进入“设定”，填写主角原名、改写后姓名、身材体型、改写模式和额外要求。
4. 建议先处理一个批次：点击“分析”，再点击“改写”，确认效果稳定后再使用“一键分析改写”。
5. 如需更严格检查，可在“设置”中开启改写复检；复检会增加请求数、等待时间和 token 消耗。
6. 一键分析改写会按批次连续处理；运行中可暂停、继续或终止，限流/网络中断后也可调整设置再继续。
7. 改写完成后进入“对比”页面，可搜索、查看差异并导出 TXT；导出只包含已完成改写的章节。

温馨提示：如果发现 API 调用异常缓慢，可以尝试删除 `C:\Users\你的用户名\AppData\Roaming\com.local.yurirewrite` 文件夹后重试。删除前请确认里面没有你仍需要保留的本地数据。

## 主要功能

### 批次处理

章节小说默认每 30 章组成一个批次。你可以只处理当前批次，也可以从当前批次开始一键处理到结尾。

### 原文分析

“分析”只整理原文设定，不会提前加入百合改写要求。分析结果包括人物、性别、称谓、关系、地点、伏笔、术语和姓名映射等一致性资料。

### 改写

“改写”会结合小说设定、核心设定和一致性资料生成改写稿。主角姓名、代词、称谓、外貌、互动和关系推进会按设定处理；未指定性转的角色、动物、灵兽等默认保持原文性别和称谓。

### 改写复检

复检会让审查模型检查改写稿，发现主角男性残留、缺句、重复、串章、逻辑冲突或设定不一致时，会要求改写模型修复。复检最多三次审查、两次修复，质量更稳，但更慢、更耗 token。

### 对比与导出

对比页可以查看左侧原文和右侧改写稿，支持全局搜索、原文/改写稿范围筛选、差异高亮和章节跳转。导出只包含已经完成改写且内容非空的章节，不会用原文补齐失败章节。

## 常见问题

### 模型诊断失败

优先检查 Base URL、模型 ID、API Key、账户额度和服务商地区限制。诊断结果会尽量保留服务商返回的错误信息。

### API 调用很慢

常见原因是网络不稳定、服务商排队、并发过高、模型思考时间过长或本地应用数据异常。可以先降低并发、关闭思考模式或换模型。如果仍然异常缓慢，可尝试删除本机应用数据目录 `C:\Users\你的用户名\AppData\Roaming\com.local.yurirewrite` 后重新配置。

### 出现 HTTP 429、TPM 或 RPM 限流

这是服务商限制请求数或 token 速率。程序会自动等待重试；重试耗尽后，一键任务会暂停。你可以降低并发、等待额度恢复、关闭复检或切换模型后点击继续。

### 审查决策 JSON 无法解析

这通常是复检模型没有稳定输出合法 JSON。程序会提示可以手动重试。频繁出现时，建议为复检选择 JSON 输出更稳定的模型，或降低并发后再试。

### 出现章节 marker 或输出截断错误

程序依靠稳定的章节开始/结束标记把批量回复拆回章节。模型输出不完整时会自动重试并缩小分片范围；如果仍失败，程序不会把无法可靠解析的内容写入章节。可尝试降低并发、减少处理范围或换用最大输出更高的模型。

### 累计 TXT 被占用

如果一键任务更新累计 TXT 时发现文件被阅读器或编辑器占用，程序会提示你手动关闭占用文件，然后继续重试。程序不会自动关闭外部软件。

## 本地数据与隐私

- 小说、批次、设定、改写稿、日志和导出文件保存在本机。
- API Key 优先保存到 Windows Credential Manager；系统凭据库不可用时才使用 SQLite 明文兜底，并会在界面提示。
- AI 日志可能包含提示词、小说片段、模型输出和服务商响应，但不会记录 API Key 或 Authorization 请求头。
- 调用在线模型时，相关小说片段会发送给你选择的 AI 服务商，请先确认其隐私政策和使用条款。
- 删除小说前，程序会列出将一起删除的数据并要求确认。

重要小说请自行保留原始 TXT 和导出的改写 TXT。

## 免责声明

本项目仅供学习、研究和个人文本处理使用。请确保你有权处理导入文本，并遵守所使用 AI 服务商条款、适用法律及版权规定。模型输出可能不准确、不完整或不符合预期，重要结果请人工检查。
